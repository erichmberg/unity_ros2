# Neon Life

A small Python terminal project: a colorful, animated **Conway's Game of Life**.

## Run

```bash
cd "Personal projects/neon-life"
python3 neon_life.py
```

## Controls

- `r` = reseed a new random world
- `Ctrl+C` = quit

No external dependencies (standard library only).
